/*
import static org.junit.jupiter.api.Assertions.*;

class SandTest {

    @org.junit.jupiter.api.Test
    void givePoints() {
        Sand s = new Sand();
        s.addWater();
        s.addWater();
        PointCounter p = new PointCounter();
        s.givePoints(p);
        assertEquals(p.getSaboteurPoints(), 2);
    }
}

 */